name: Run GitHub issue copilot
on:
  issues:
    types: [opened, edited, closed]

jobs:
  Issue:
     permissions:
        issues: write
     runs-on: ubuntu-latest
     steps:
     - name: Run GitHub issue copilot
       uses: MoChilia/IssueCopilot@768eb62c2889b4ad5932342e99ae8300849f7593
       with:
         password: ${{secrets.COPILOT_PASSWORD}}
